[package]
name = "workflow-verifier-vm-agent"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[lib]
path = "src/lib.rs"

[[bin]]
name = "workflow-verifier-vm-agent"
path = "src/main.rs"

[dependencies]
workflow-verifier-internal = { package = "workflow-verifier", path = "../..", version = "=0.1.0", default-features = false, features = ["internal-support"] }

[target.'cfg(target_os = "linux")'.dependencies]
libc = "=0.2.189"

[lints]
workspace = true
