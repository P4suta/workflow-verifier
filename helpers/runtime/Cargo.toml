[package]
name = "workflow-verifier-helper-runtime"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
workflow-verifier-runner-protocol = { path = "../protocol", version = "=0.1.0" }

[target.'cfg(windows)'.dependencies]
same-file = "=1.0.6"

[lints]
workspace = true
