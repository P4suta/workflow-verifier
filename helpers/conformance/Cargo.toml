[package]
name = "workflow-verifier-helper-conformance"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
workflow-verifier-runner-protocol = { path = "../protocol" }
workflow-verifier-linux-helper = { path = "../linux" }
workflow-verifier-windows-helper = { path = "../windows" }
workflow-verifier-macos-helper = { path = "../macos" }

[lints]
workspace = true
