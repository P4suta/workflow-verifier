[package]
name = "workflow-verifier-conformance"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[[bin]]
name = "workflow-verifier-conformance"
path = "src/main.rs"

[dependencies]
workflow-verifier-internal = { package = "workflow-verifier", path = "../..", version = "=0.1.0", default-features = false, features = ["conformance-support"] }

[lints]
workspace = true
