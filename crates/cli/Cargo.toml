[package]
name = "workflow-verifier-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[[bin]]
name = "workflow-verifier"
path = "src/main.rs"

[dependencies]
rustls.workspace = true
rustls-native-certs.workspace = true
serde.workspace = true
serde_json.workspace = true
toml.workspace = true
url.workspace = true
zeroize.workspace = true
workflow-verifier-domain = { path = "../domain", version = "=0.1.0" }
workflow-verifier-engine = { path = "../engine", version = "=0.1.0" }
workflow-verifier-foundation = { path = "../foundation", version = "=0.1.0" }
workflow-verifier-frontend = { path = "../frontend", version = "=0.1.0" }
workflow-verifier-helper-runtime = { path = "../../helpers/runtime", version = "=0.1.0" }
workflow-verifier-product = { path = "../product", version = "=0.1.0" }
workflow-verifier-sandbox = { path = "../sandbox", version = "=0.1.0" }
workflow-verifier-syntax = { path = "../syntax", version = "=0.1.0" }
workflow-verifier-verifier = { path = "../verifier", version = "=0.1.0" }

[lints]
workspace = true
