[package]
name = "workflow-verifier-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[[bin]]
name = "workflow-verifier"
path = "src/main.rs"

[dependencies]
rustls.workspace = true
rustls-native-certs.workspace = true
serde.workspace = true
serde_json.workspace = true
toml.workspace = true
url.workspace = true
zeroize.workspace = true
workflow-verifier-domain = { path = "../domain" }
workflow-verifier-engine = { path = "../engine" }
workflow-verifier-foundation = { path = "../foundation" }
workflow-verifier-frontend = { path = "../frontend" }
workflow-verifier-helper-runtime = { path = "../../helpers/runtime" }
workflow-verifier-product = { path = "../product" }
workflow-verifier-sandbox = { path = "../sandbox" }
workflow-verifier-syntax = { path = "../syntax" }
workflow-verifier-verifier = { path = "../verifier" }

[lints]
workspace = true
