name: Docs (GitHub Pages)

on:
  push:
    branches: [ main ]
    paths:
      - 'Sources/**'
      - 'README.md'
      - 'Package.swift'
      - 'Package.resolved'
      - '.github/workflows/docs.yml'
      - '.github/workflows/docc-generate.yml'
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: false

jobs:
  build:
    uses: ./.github/workflows/docc-generate.yml
    with:
      docs-domain: 'swiftftr.networkweather.com'

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
